import { CanShowDetailsPipe } from './can-show-details.pipe';

describe('CanShowDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new CanShowDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
